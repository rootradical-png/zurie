# Config
config.php
.env
config/*.php
!config/*.example

# Runtime data
data/*.json
map/data/*.json

# Logs / cache / session
*.log
*.session
*.cache

# Uploads / backups
upload/
uploads/
backup/
*.zip
*Copy.htaccess

# Dependencies
vendor/
node_modules/
# SFTP photo version runtime cache
/data/photo_version_cache/
/data/photo_versions_work/

# SFTP photo archive: keep protection file, ignore archived student images/logs
/archive/sftp_photos/*
!/archive/sftp_photos/.htaccess
!/archive/sftp_photos/README.txt
